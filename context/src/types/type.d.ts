interface productType {
  id: string;
  image: string;
  title: string;
  price: number;
  description: string;
}

interface itemType {
  id: string;
  name: string;
  price: number;
  quantity: number;
}
