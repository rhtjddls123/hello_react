interface itemType {
  id: string;
  image: string;
  title: string;
  price: number;
  description: string;
}

interface cartType {
  id: string;
  name: string;
  price: number;
  quantity: number;
}
